import { Card } from '@mui/material';
import { styled } from '@mui/system';

const StyledCard = styled(Card)({
  minHeight: '300px',
  margin: 2,
  backgroundColor: 'rgba(254, 250, 250, 0.85)',
  borderRadius: 6,
  boxShadow: 4,
});

export default StyledCard;